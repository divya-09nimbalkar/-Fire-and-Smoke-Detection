# -Fire-and-Smoke-Detection
This project focuses on detecting fire and smoke in images using deep learning (PyTorch).   It provides a complete pipeline for data preprocessing, model training, evaluation, and inference.
